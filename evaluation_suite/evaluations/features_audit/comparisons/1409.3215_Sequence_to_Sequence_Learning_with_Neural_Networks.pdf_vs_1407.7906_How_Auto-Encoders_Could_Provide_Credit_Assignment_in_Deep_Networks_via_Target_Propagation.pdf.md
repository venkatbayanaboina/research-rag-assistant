| Dimension | 1409.3215_Sequence_to_Sequence_Learning_with_Neural_Networks.pdf | 1407.7906_How_Auto-Encoders_Could_Provide_Credit_Assignment_in_Deep_Networks_via_Target_Propagation.pdf |  
| --- | --- | --- |  
| **Core Architecture** | Uses two deep LSTMs: one for encoding input sequences into a fixed-dimensional vector and another for decoding the output sequence. The input LSTM processes sequences in reverse to simplify optimization. | Proposes a deep auto-encoder architecture with encoder and decoder paths. Emphasizes reconstruction as a layer-local training signal, with target propagation to handle discrete or non-differentiable operations. |  
| **Key Formulas & Math Approach** | Defines the sequence-to-sequence probability as $ p(y_1,...,y_{T'}|x_1,...,x_T) = \prod_{t=1}^{T'} p(y_t|v, y_1,...,y_{t-1}) $, where $ v $ is the input sequence's fixed-dimensional representation. Uses beam search for decoding. | Introduces target propagation as a generalization of backpropagation, using auto-encoder reconstructions as targets. Focuses on optimizing $ \partial \log P(y, h(x)) / \partial h(x) $ for credit assignment, enabling handling of discrete variables. |  
| **Main Contributions** | Demonstrates end-to-end sequence-to-sequence learning with LSTMs, achieving competitive BLEU scores (34.81) on WMT’14 translation. Introduces reversing input sequences to improve optimization. Highlights robustness to long sentences and phrase/sentence representation learning. | Proposes auto-encoder-mediated target propagation as an alternative to backpropagation, enabling training of deep networks with discrete units. Suggests applications to generative models, multi-modal learning, and biologically plausible credit assignment. |  
| **Key Results & Tables** | Reports BLEU scores (34.81 for direct translation, 36.5 for rescoring SMT hypotheses). Shows improved performance on long sentences after reversing input sequences. Includes qualitative examples of translation invariance to active/passive voice. | No specific numerical results provided. Focuses on theoretical framework and conjectures about target propagation's potential for handling discrete variables and long-term dependencies. |  
| **Limitations** | Relies on reversing input sequences for optimization, which may not generalize to all tasks. Limited vocabulary (80k words) penalizes out-of-vocabulary words. Beam search is approximate and computationally intensive. | Theoretical and lacks empirical validation. Challenges include handling discrete variables, scalability, and integration with existing training paradigms (e.g., backpropagation). |  

**Summary of Differences**:  
1. **Objective**: Paper A focuses on sequence-to-sequence learning for tasks like translation, while Paper B explores auto-encoder-based credit assignment for deep networks.  
2. **Methodology**: Paper A uses LSTMs with reversed input sequences, whereas Paper B introduces target propagation via auto-encoders to address credit assignment challenges.  
3. **Applications**: Paper A is applied to machine translation, while Paper B targets generative models and biologically plausible learning mechanisms.